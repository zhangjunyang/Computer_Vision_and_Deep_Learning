dist: trusty
sudo: required
language: python
python:
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install tox-travis
# command to run tests
script:
  - pytest # or py.test for Python versions 3.6 and below
